import styled from 'styled-components';

const SectionStyled = styled.section`
  border-bottom: 2px solid ${props => props.theme.colors.blue3};
`;

const MainNavigationStyles = {
  SectionStyled,
};

export default MainNavigationStyles;
